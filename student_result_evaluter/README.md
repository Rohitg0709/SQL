# Student Result Evaluator (Python)

A simple Python program that evaluates a student's result based on marks entered by the user.

## 📌 Project Description
This program takes marks (out of 100) as input and classifies the result into different categories using conditional statements.

## 📊 Evaluation Criteria
- Marks less than 0 or greater than 100 → Invalid marks
- Marks less than 35 → Fail
- Marks from 35 to 49 → Pass
- Marks from 50 to 74 → Good
- Marks from 75 to 100 → Excellent

## 🧠 Concepts Used
- Variables
- User input (`input()`)
- Type conversion (`int()`)
- Conditional statements (`if`, `elif`, `else`)

## ▶️ How to Run
1. Make sure Python is installed
2. Open a terminal in the project folder
3. Run the program using:

```bash
python result.py
